spring:
  application:
    name: jangsu
  datasource:
    url: jdbc:mysql://10.20.151.86:3306/test
    username: rocky1
    password: P@ssw0rd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # 또는 create, create-drop 등
    show-sql: true  # SQL 쿼리 로그를 보기 위해 추가
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

    
      

